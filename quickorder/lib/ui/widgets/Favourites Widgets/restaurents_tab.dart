import 'package:flutter/material.dart';

class FavRestaurents extends StatelessWidget {
  const FavRestaurents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
