class CategoryModel {
  int? id;
  String imagePath;
  bool isSelected;
  String? name;

  CategoryModel({
    required this.id,
    required this.name,
    required this.imagePath,
    required this.isSelected,
  });
}

class RestaurentModele {
  int? id;
  String imagePath;
  bool isSelected;
  String? name;

  RestaurentModele({
    required this.id,
    required this.name,
    required this.imagePath,
    required this.isSelected,
  });
}
